import React from 'react';
import UnderConstructionPage from '@/components/UnderConstructionPage';

const Events: React.FC = () => {
  return <UnderConstructionPage title="Events" />;
};

export default Events;