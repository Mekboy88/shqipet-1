import React from 'react';
import UnderConstructionPage from '@/components/UnderConstructionPage';

const Albums: React.FC = () => {
  return <UnderConstructionPage title="Albums" />;
};

export default Albums;